# Config TODO

## Future configuration improvements

- [ ] Make Feature Engineering pipeline configurable.
      - Add FeatureEngineerFactory.
      - Load engineers from config.

- [ ] Add configurable TrainingStrategy.
      - Create TrainingStrategyFactory when multiple strategies exist.

- [ ] Add configurable EvaluationStrategy.
      - Create EvaluationStrategyFactory when multiple strategies exist.

## when features implemented in the future
- [ ] Add configurable output paths.

- [ ] Add configurable logging.

- [ ] Add configurable random seed.

- [ ] Add configurable cross-validation.

- [ ] Add configurable hyperparameter search.